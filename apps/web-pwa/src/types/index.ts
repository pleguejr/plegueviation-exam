/**
 * Tipos de la PWA — reexporta el núcleo compartido y mantiene extensiones locales mínimas.
 */
export type {
  Option,
  Annex,
  Explanation,
  QuestionMetadata,
  Question,
  DeletedQuestion,
  ReviewRequest,
  ExamMode,
  ExamSelectionStrategy,
  ExamConfig,
  AnswerHistoryEntry,
  QuestionStats,
  ExamSessionAnswer,
  ExamSession,
  CategoryMetadata,
  BankManifest,
  CompactExamSession,
  SyncPayload,
  CategorySummary
} from '@plegue/core-engine';
